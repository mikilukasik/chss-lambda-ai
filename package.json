{
  "name": "chss-lambda-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "install-submodules": "git submodule update --init --recursive && git submodule foreach npm i --recursive"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.2.0",
    "aws-sdk": "^2.1353.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114"
  }
}
